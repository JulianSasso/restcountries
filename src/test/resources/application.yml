spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/
      database: restcountries
      collection: countries

repository:
  type: mongodb
#  type: json
  json:
    filePath: countriesTest.json